// import {Repository} from '../repository';
// import { Users } from './users';
export class Repository {
  constructor (public profile: any, public login: string, public public_repos: number, public html_url: string) {
  }
}
